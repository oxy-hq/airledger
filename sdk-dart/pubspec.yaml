name: airledger_engine
description: Dart bindings for the airledger-engine schema + ingest core.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  test: ^1.25.0
